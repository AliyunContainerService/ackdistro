# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
images:
  npd:
    image: ecp_builder/node-problem-detector
    tag: v0.2
k8s_namespace: kube-system
globalconfig:
  RegistryURL: sea.hub:5000
