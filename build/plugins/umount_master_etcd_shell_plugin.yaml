apiVersion: sealer.aliyun.com/v1alpha1
kind: Plugin
metadata:
  name: umount-master-etcd # Specify this plugin name,will dump in $rootfs/plugin dir.
spec:
  type: SHELL
  action: PreClean # PreInit PreInstall PostInstall
  'on': master
  data: |
    set -e; set -x
    umount /var/lib/etcd
