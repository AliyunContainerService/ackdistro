apiVersion: sealer.aliyun.com/v1alpha1
kind: Plugin
metadata:
  name: MyShell # Specify this plugin name,will dump in $rootfs/plugin dir.
spec:
  type: SHELL
  action: PostClean # PreInit PreInstall PostInstall
  data: |
    /bin/sh scripts/disk_init_rollback.sh