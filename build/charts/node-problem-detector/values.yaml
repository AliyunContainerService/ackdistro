# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
images:
  npd:
    image: node-problem-detector/node-problem-detector
    tag: v0.8.7
k8s_namespace: kube-system
globalconfig:
  RegistryURL: k8s.gcr.io