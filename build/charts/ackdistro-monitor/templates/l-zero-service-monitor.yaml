apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: l-zero
  namespace: {{ .Release.Namespace }}
spec:
  endpoints:
  - path: /metrics
    port: metrics
  jobLabel: app
  namespaceSelector:
    matchNames:
    - kube-system
  selector:
    matchLabels:
      app: l-zero