# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
images:
  dingtalk:
    image: ecp_builder/prometheus-webhook-dingtalk
    tag: v0.1
k8s_namespace: kube-system
globalconfig:
  DisableFailureDomain: false
  RegistryURL: sea.hub:5000