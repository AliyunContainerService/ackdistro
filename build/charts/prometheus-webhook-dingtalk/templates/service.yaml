apiVersion: v1
kind: Service
metadata:
  name: dingtalk-service
spec:
  type: NodePort
  selector:
    app: dingtalk-app
  ports:
    - port: 8060
      targetPort: 8060
      nodePort: 30025
      protocol: TCP